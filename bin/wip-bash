#!/usr/bin/env bash

cmd="$1"

# echo "wip-bash..."

wip-show() {
  # echo "    show...  name: $1"
}

case "$cmd" in
  show)
    wip-show $2
    ;;
  *)
    wip-show $1
    ;;
esac



# for wiprc in "$HOME/.wiprc" "./.wiprc" ; do
#   
# done
